// Site configuration. `repo` is the GitHub owner/repo used by the submit form
// to open pre-filled issues. Update it once the remote repository exists (Task 12).
export const repo = 'chinadbo/fairarchive';
